# Public API base URL (used by both the public site service layer and the admin axios client)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Cache purge webhook secret (must match server REVALIDATE_SECRET)
REVALIDATE_SECRET=paste-the-same-secret-here
